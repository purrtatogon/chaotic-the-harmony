package com.java.backend.model.enums;

/** Persisted as enum name strings on {@code app_users.role}. */
public enum Role {
    CUSTOMER,
    ADMIN,
    MANAGER,
    STAFF,
    SUPPORT,
    AUDITOR;
}
