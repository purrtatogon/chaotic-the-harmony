import remarkGfm from 'remark-gfm';

/** Use with `react-markdown` for strikethrough, tables, autolink, task lists (GitHub-Flavored Markdown). */
export const productMarkdownRemarkPlugins = [remarkGfm];
