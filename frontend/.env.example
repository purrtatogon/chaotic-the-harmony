# Copy to .env and/or .env.development / .env.production as needed (see frontend README).
# Never commit real keys.

VITE_API_URL=http://localhost:8080/api/v1

# Optional: only if your tooling needs it (replace with your own key or remove).
# VITE_PUBLIC_BUILDER_KEY=
